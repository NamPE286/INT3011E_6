# Used only when running src/index.ts directly. Wrangler defaults to port 8787.
PORT=8787

# Keep Cloudflare credentials local. Wrangler also accepts these environment variables.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
